#SPDX-License-Identifier MIT-0
---
# tasks file for roles/file_creator
- name: Create file using my_own_module
  my_own_namespace.yandex_cloud_elk.my_own_module:
    path: "{{ file_path }}"
    content: "{{ file_content }}"
